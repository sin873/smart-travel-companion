# 智能伴旅小程序 - 个性化行程规划Agent开发文档

## 📋 项目概述

**项目名称**: 智能伴旅小程序 - 个性化行程规划Agent
**开发分支**: feature/miniprogram-agent
**核心功能**: 基于AI Agent的个性化行程定制与规划

---

## 🎯 核心功能规划

### 小程序端功能

| 功能模块 | 描述 | 优先级 |
|---------|------|--------|
| 行程定制表单 | 目的地、时间、人数、偏好等 | P0 |
| AI Agent规划 | 基于RAG知识库的智能行程生成 | P0 |
| 行程展示 | 每日行程、景点详情、路线规划 | P0 |
| 行程调整 | 拖拽调整、增删景点 | P1 |
| 行程保存 | 保存到本地、分享给好友 | P1 |
| 实时咨询 | 基于RAG的景点问答 | P1 |

### 后端Agent功能

| 功能模块 | 描述 | 优先级 |
|---------|------|--------|
| 行程规划Agent | 协调多个工具生成完整行程 | P0 |
| 景点信息检索 | 基于RAG的景点知识库查询 | P0 |
| 路线优化 | 基于地理位置的路线规划 | P1 |
| 偏好学习 | 记录用户偏好,优化推荐 | P1 |

---

## 🏗️ 系统架构

```
智能伴旅小程序架构
├── 小程序端 (微信小程序)
│   ├── 页面层
│   │   ├── 首页/登录页
│   │   ├── 行程定制页
│   │   ├── 行程展示页
│   │   └── 我的行程页
│   ├── 组件层
│   │   ├── 行程卡片
│   │   ├── 景点列表
│   │   └── 时间轴
│   └── 服务层
│       ├── API封装
│       ├── 状态管理
│       └── 本地存储
├── 后端扩展 (Spring Boot)
│   ├── Controller层
│   │   ├── 行程规划API
│   │   └── 小程序用户API
│   ├── Service层
│   │   ├── TravelAgentService (Agent核心)
│   │   ├── ItineraryService (行程管理)
│   │   └── RouteOptimizationService (路线优化)
│   └── Entity层
│       ├── Itinerary (行程)
│       ├── ItineraryDay (每日行程)
│       └── Attraction (景点)
└── 复用现有模块
    ├── RAG知识库检索
    ├── 用户认证 (JWT)
    └── WebSocket实时对话
```

---

## 🗄️ 数据库设计扩展

### 5. 用户行程表 (itinerary)

```sql
CREATE TABLE itinerary (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    itinerary_id VARCHAR(64) UNIQUE NOT NULL COMMENT '行程ID',
    user_id VARCHAR(64) NOT NULL COMMENT '用户ID',
    title VARCHAR(256) NOT NULL COMMENT '行程标题',
    destination VARCHAR(128) NOT NULL COMMENT '目的地',
    start_date DATE NOT NULL COMMENT '开始日期',
    end_date DATE NOT NULL COMMENT '结束日期',
    traveler_count INT DEFAULT 1 COMMENT '旅行人数',
    budget DECIMAL(10,2) COMMENT '预算',
    preferences JSON COMMENT '用户偏好 (JSON)',
    status VARCHAR(32) DEFAULT 'DRAFT' COMMENT '状态: DRAFT/PLANNED/COMPLETED',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_destination (destination),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户行程表';
```

### 6. 每日行程表 (itinerary_day)

```sql
CREATE TABLE itinerary_day (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    day_id VARCHAR(64) UNIQUE NOT NULL COMMENT '每日行程ID',
    itinerary_id VARCHAR(64) NOT NULL COMMENT '所属行程ID',
    day_number INT NOT NULL COMMENT '第几天',
    date DATE NOT NULL COMMENT '日期',
    title VARCHAR(256) COMMENT '当日主题',
    notes TEXT COMMENT '备注',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_itinerary_id (itinerary_id),
    INDEX idx_day_number (day_number)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='每日行程表';
```

### 7. 行程景点表 (itinerary_attraction)

```sql
CREATE TABLE itinerary_attraction (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    item_id VARCHAR(64) UNIQUE NOT NULL COMMENT '行程项ID',
    day_id VARCHAR(64) NOT NULL COMMENT '所属每日行程ID',
    attraction_id VARCHAR(64) COMMENT '景点ID',
    attraction_name VARCHAR(256) NOT NULL COMMENT '景点名称',
    address VARCHAR(512) COMMENT '地址',
    latitude DECIMAL(10,7) COMMENT '纬度',
    longitude DECIMAL(10,7) COMMENT '经度',
    start_time TIME COMMENT '开始时间',
    end_time TIME COMMENT '结束时间',
    duration_minutes INT COMMENT '预计时长(分钟)',
    order_index INT NOT NULL COMMENT '排序',
    notes TEXT COMMENT '备注',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_day_id (day_id),
    INDEX idx_order_index (order_index)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='行程景点表';
```

---

## 🔌 API接口规范 (小程序专用)

### 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1234567890
}
```

### 1. 小程序登录

#### 1.1 微信登录
**POST** `/api/v1/miniprogram/auth/login`

```typescript
// Request
interface WxLoginRequest {
  code: string;           // 微信登录code
  userInfo?: {
    nickName: string;
    avatarUrl: string;
  };
}

// Response
interface WxLoginResponse {
  token: string;           // JWT token
  userId: string;
  isNewUser: boolean;
}
```

### 2. 行程规划API

#### 2.1 创建行程规划任务
**POST** `/api/v1/miniprogram/itinerary/plan`

```typescript
// Request
interface PlanItineraryRequest {
  destination: string;           // 目的地
  startDate: string;             // 开始日期 (YYYY-MM-DD)
  endDate: string;               // 结束日期 (YYYY-MM-DD)
  travelerCount: number;         // 人数
  budget?: number;               // 预算
  preferences: {
    pace: 'relaxed' | 'moderate' | 'intensive';  // 节奏
    interests: string[];         // 兴趣: historical, natural, cultural, shopping, food
    avoidCrowds?: boolean;       // 避开人群
    includeMeals?: boolean;      // 包含餐饮推荐
  };
}

// Response (异步,返回任务ID)
interface PlanItineraryResponse {
  taskId: string;
  status: 'QUEUED' | 'PROCESSING';
  estimatedTime: number;  // 预计耗时(秒)
}
```

#### 2.2 查询规划任务状态
**GET** `/api/v1/miniprogram/itinerary/plan/{taskId}`

```typescript
// Response
interface PlanTaskStatusResponse {
  taskId: string;
  status: 'QUEUED' | 'PROCESSING' | 'COMPLETED' | 'FAILED';
  progress: number;  // 0-100
  message?: string;
  itinerary?: ItineraryDTO;  // 完成时返回
}
```

#### 2.3 保存行程
**POST** `/api/v1/miniprogram/itinerary`

```typescript
// Request
interface SaveItineraryRequest {
  title: string;
  destination: string;
  startDate: string;
  endDate: string;
  travelerCount: number;
  days: ItineraryDayDTO[];
}

// Response
interface SaveItineraryResponse {
  itineraryId: string;
}
```

#### 2.4 获取行程列表
**GET** `/api/v1/miniprogram/itinerary`

```typescript
// Response
interface ItineraryListResponse {
  total: number;
  items: ItineraryDTO[];
}
```

#### 2.5 获取行程详情
**GET** `/api/v1/miniprogram/itinerary/{itineraryId}`

```typescript
// Response
interface ItineraryDetailResponse {
  itinerary: ItineraryDTO;
  days: ItineraryDayDTO[];
}
```

---

## 📱 小程序页面结构

```
miniprogram/
├── pages/
│   ├── index/              # 首页
│   ├── login/              # 登录页
│   ├── plan/               # 行程定制页
│   ├── planning/           # 规划中页面
│   ├── itinerary/          # 行程展示页
│   ├── itinerary-detail/   # 行程详情页
│   └── my-itineraries/     # 我的行程
├── components/
│   ├── itinerary-card/     # 行程卡片
│   ├── day-timeline/       # 每日时间轴
│   ├── attraction-item/    # 景点项
│   └── plan-form/          # 规划表单
├── utils/
│   ├── api.js              # API封装
│   ├── auth.js             # 认证工具
│   └── storage.js          # 本地存储
├── app.js
├── app.json
└── project.config.json
```

---

## 🤖 AI Agent设计

### Travel Agent 工作流

```
用户行程请求
    ↓
[意图理解] 解析用户需求
    ↓
[信息收集] 从RAG检索目的地信息
    ↓
[景点筛选] 基于偏好筛选景点
    ↓
[路线规划] 优化每日行程路线
    ↓
[时间安排] 分配各景点时间
    ↓
[行程生成] 生成完整行程方案
    ↓
[方案优化] 根据反馈调整
```

### Agent 核心类

```java
@Service
public class TravelAgentService {
    
    // 主规划方法
    public Itinerary planItinerary(ItineraryPlanRequest request);
    
    // 步骤1: 检索景点信息
    private List<AttractionInfo> searchAttractions(String destination);
    
    // 步骤2: 筛选景点
    private List<AttractionInfo> filterAttractions(List<AttractionInfo> attractions, UserPreferences preferences);
    
    // 步骤3: 规划路线
    private List<ItineraryDay> optimizeRoute(List<AttractionInfo> attractions, int days);
    
    // 步骤4: 调用LLM生成描述
    private String generateDayDescription(ItineraryDay day);
}
```

---

## 🚀 开发计划

### 阶段1: 后端Agent开发 (1-2天)
- [ ] 创建数据库表
- [ ] 实现TravelAgentService核心逻辑
- [ ] 实现行程规划API
- [ ] 集成RAG知识库检索

### 阶段2: 小程序基础框架 (1天)
- [ ] 初始化小程序项目
- [ ] 配置项目结构
- [ ] 实现登录流程
- [ ] 封装API请求

### 阶段3: 行程定制与展示 (1-2天)
- [ ] 实现行程定制表单
- [ ] 实现规划进度页面
- [ ] 实现行程展示页面
- [ ] 实现行程详情页面

### 阶段4: 优化与测试 (1天)
- [ ] 性能优化
- [ ] 用户体验优化
- [ ] 测试与修复bug

---

## 📝 参考资料

- 微信小程序官方文档
- Vant Weapp组件库
- 现有智能伴旅后端代码
- 派聪明RAG实现
