# 小程序重构版

这是独立于旧 `miniprogram` 的全新小程序目录。

目标：
- 先跑通最小业务闭环
- 避免旧目录中的模板损坏和编码污染
- 页面职责单一：首页、登录、填写需求、生成进度、行程详情、我的行程

使用方式：
1. 用微信开发者工具打开 `小程序重构版/miniprogram`
2. 清缓存后重新编译
3. 联调前先启动后端 `http://localhost:8081/api/v1/travel`
